# Predecessor captures — not final evidence

These two tracked PNGs belonged to earlier Task 13 fix rounds. They are retained
for provenance and are not referenced by the active golden tests or final matrix.
Use `../final_home_empty_390_1.0.png` and `../final_rest_390_1.0.png` for the
final typed-fixture captures. Other `final_*.png` files form the 74-image final
host set. No file in either set represents Android-device acceptance.
